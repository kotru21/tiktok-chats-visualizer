# Tiktok Chats Visualizer

[English version](./README.en.md)

![Приложение в тёмной теме](https://github.com/user-attachments/assets/f8ddb96b-2332-412a-893d-72cf20a82f3c)

## 📱 О проекте

Tiktok Chats Visualizer - это веб-приложение для анализа и визуализации истории чатов из экспортированных данных TikTok. Приложение позволяет загрузить JSON-файл с историей переписки и отображает подробную статистику вашего общения.

## ✨ Возможности

- Загрузка и анализ JSON-файлов с историей чатов TikTok
- Отображение списка пользователей, с которыми вы общались
- Подробная статистика для каждого чата:
  - Общее количество сообщений
  - Среднее количество сообщений в день
  - Распределение сообщений между участниками
  - Наиболее часто используемые слова и словосочетания
  - Активность общения по датам
  - Анализ по дням недели и времени суток
- Автоматическая тёмная тема, которая адаптируется к системным настройкам устройства
- Модульная архитектура с поддержкой ES6 модулей
- Интерактивные графики с возможностью настройки

![Приложение в светлой теме](https://github.com/user-attachments/assets/40b53f56-eea8-499d-a132-c1fd4a5ebe94)

## 🛠 Технологии

- **Frontend**: HTML, CSS, TypeScript/JavaScript (ES6+ модули), Chart.js, Vite
- **Backend**: Node.js, Express, TypeScript
- **Дополнительные библиотеки**: Moment.js
- **Тестирование**: Mocha с поддержкой ES-модулей
- **Code Quality**: ESLint, Prettier, TypeScript strict mode

## 🚀 Установка и запуск

### Требования

- Node.js (версия 18 или выше)
- npm (устанавливается вместе с Node.js)
- экспортированные сообщения TikTok в формате JSON (Подробнее в разделе "как использовать")
- для хостинга в production требуется установить { secure: true } в настройке сессий

### Шаги установки

1. Клонируйте репозиторий:

```bash
git clone https://github.com/your-username/tiktok-chats-visualizer.git
cd tiktok-chats-visualizer
```

Также вы можете скачать архив с кодом через интерфейс GitHub.

1. Установите зависимости:

```bash
npm install
```

1. Запустите в режиме разработки:

```bash
# Запуск сервера и клиента одновременно
npm run dev

# Или по отдельности:
npm run start:dev    # Только сервер (с hot-reload)
npm run dev:client   # Только клиент Vite
```

1. Для production сборки:

```bash
npm run build        # Сборка сервера и клиента
npm start            # Запуск production сервера
```

1. Откройте в браузере:
   - Разработка: <http://localhost:3001> (Vite прокси на API)
   - Production: <http://localhost:3000>

## 🧪 Тестирование

В проекте используется Mocha и ESM-модули. Запуск тестов:

```bash
npm test         # Запуск TypeScript тестов
npm run test:js  # Запуск JavaScript тестов (legacy)
```

Тесты находятся в директории `tests/` и покрывают утилиты:

- `utils/dateUtils` - функции работы с датами
- `utils/statsUtils` - статистические вычисления
- `utils/textUtils` - обработка текста
- `utils/stopWords` - фильтрация стоп-слов
- `utils/dataProcessor` - обработка данных чатов
- `public/js/charts/colorScheme` - цветовые схемы для графиков
- `public/js/theme` - управление темами приложения
- `public/js/utils/themeUtils` - утилиты для работы с темами

Если добавляете новые утилиты, создавайте соответствующие файлы `*.test.ts` в папке `tests/`.

## 💻 Разработка

### TypeScript

Проект использует TypeScript со строгой типизацией:

```bash
# Проверка типов
npm run typecheck

# Проверка типов сервера
npm run typecheck:server

# Проверка типов клиента
npm run typecheck:client
```

Конфигурация TypeScript:

- `tsconfig.json` - общая конфигурация для IDE
- `tsconfig.server.json` - конфигурация для Node.js сервера
- `tsconfig.client.json` - конфигурация для браузерного кода

Общие типы находятся в папке `types/`:

- `chat.ts` - типы сообщений и чатов
- `stats.ts` - типы статистики
- `date.ts` - типы дат и времени
- `ui.ts` - типы UI компонентов

### Code Quality

Проект использует инструменты для поддержания качества кода:

```bash
# Проверка линтером
npm run lint

# Автоматическое исправление
npm run lint:fix

# Форматирование кода с помощью Prettier
npm run format
```

### Структура модулей

- Все модули используют ES6+ синтаксис import/export с TypeScript
- Клиентские модули организованы по функциональности
- Серверные утилиты изолированы и легко тестируются
- Каждый модуль имеет четкую область ответственности
- Общие типы вынесены в папку `types/`

### Конвенции TypeScript

При контрибьютинге придерживайтесь следующих правил:

1. **Избегайте `any`** - используйте `unknown` где необходимо
2. **Явные типы возврата** - указывайте return type для функций
3. **Интерфейсы для объектов** - создавайте интерфейсы в `types/`
4. **Strict null checks** - всегда проверяйте nullable значения
5. **Literal types** - используйте `as const` для констант

## 📖 Как использовать

1. **Экспортируйте данные из TikTok**:
   - Откройте TikTok на мобильном устройстве
   - Перейдите в настройки → Аккаунт → Скачать ваши данные
   - выберите только сообщения для скачивания
   - Выберите "JSON" в качестве формата экспорта
   - Дождитесь, пока данные будут готовы к скачиванию

2. **Работа с приложением**:
   - Загрузите полученный JSON-файл через форму на главной странице
   - Выберите пользователя из списка слева
   - Просмотрите подробную статистику общения

## 🧩 Структура проекта

```text
├── public/                    # Клиентская часть
│   ├── css/
│   │   └── style.css         # Основные стили приложения
│   ├── js/
│   │   ├── charts/           # Модули для построения графиков
│   │   │   ├── authorChart.ts       # График распределения по авторам
│   │   │   ├── colorScheme.ts       # Цветовые схемы
│   │   │   ├── dateChart.ts         # График активности по датам
│   │   │   ├── pairsChart.ts        # График частых словосочетаний
│   │   │   ├── timeOfDayChart.ts    # График активности по времени дня
│   │   │   ├── weekdayChart.ts      # График активности по дням недели
│   │   │   └── wordsChart.ts        # График частых слов
│   │   ├── ui/               # Модули пользовательского интерфейса
│   │   │   ├── statsView.ts         # Отображение статистики
│   │   │   └── userList.ts          # Список пользователей
│   │   ├── utils/            # Клиентские утилиты
│   │   │   └── themeUtils.ts        # Утилиты для работы с темами
│   │   ├── api.ts            # API для работы с сервером
│   │   ├── charts.ts         # Основной модуль управления графиками
│   │   ├── config.ts         # Конфигурация приложения
│   │   ├── main.ts           # Главный модуль приложения
│   │   ├── theme.ts          # Управление темами
│   │   └── uploader.ts       # Загрузка и обработка файлов
│   └── index.html            # Главная страница
├── types/                    # Общие TypeScript типы
│   ├── chat.ts               # Типы сообщений и чатов
│   ├── date.ts               # Типы дат
│   ├── stats.ts              # Типы статистики
│   ├── ui.ts                 # Типы UI компонентов
│   └── index.ts              # Реэкспорт всех типов
├── utils/                    # Серверные утилиты
│   ├── dataProcessor.ts      # Обработка данных чатов
│   ├── dateUtils.ts          # Функции работы с датами
│   ├── statsUtils.ts         # Статистические вычисления
│   ├── stopWords.ts          # Фильтрация стоп-слов
│   └── textUtils.ts          # Обработка текста
├── tests/                    # Тесты
│   ├── colorScheme.test.ts   # Тесты цветовых схем
│   ├── dataProcessor.test.ts # Тесты обработки данных
│   ├── dateUtils.test.ts     # Тесты функций дат
│   ├── statsUtils.test.ts    # Тесты статистики
│   ├── stopWords.test.ts     # Тесты стоп-слов
│   ├── textUtils.test.ts     # Тесты обработки текста
│   ├── theme.test.ts         # Тесты тем
│   └── themeUtils.test.ts    # Тесты утилит тем
├── dist/                     # Скомпилированный код (gitignore)
│   ├── server/               # Серверный код
│   └── client/               # Клиентский бандл
├── server.ts                 # Express сервер
├── vite.config.ts            # Конфигурация Vite
├── tsconfig.json             # Общая конфигурация TypeScript
├── tsconfig.server.json      # TypeScript для сервера
├── tsconfig.client.json      # TypeScript для клиента
├── package.json              # Зависимости и скрипты
├── eslint.config.js          # Конфигурация ESLint
└── README.md                 # Документация
```

## 🏗 Архитектура

Проект использует модульную архитектуру с разделением ответственности:

### Frontend (TypeScript + Vite)

- **charts/** - специализированные модули для каждого типа графика
- **ui/** - компоненты пользовательского интерфейса
- **utils/** - вспомогательные функции для клиента
- **main.ts** - точка входа, координирует работу всех модулей
- Vite обеспечивает hot-reload и оптимизированную сборку

### Backend (TypeScript + Node.js)

- **utils/** - серверные утилиты для обработки данных
- **server.ts** - Express сервер с API endpoints
- tsx для запуска TypeScript напрямую

### Types (Общие типы)

- **types/** - интерфейсы и типы, используемые на сервере и клиенте
- Строгая типизация с `strict: true`

### Testing

- Полное покрытие тестами всех утилит
- Поддержка ES-модулей в тестах
- Отдельные тесты для клиентских и серверных компонентов
